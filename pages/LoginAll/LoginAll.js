import { StyleSheet, Text, View } from 'react-native';

function LoginAll() {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
          <Text>LoginAll Screen</Text>
        </View>
      );
}

export default LoginAll;